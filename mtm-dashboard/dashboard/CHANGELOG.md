# MtM Dashboard (risk-console) — 버전 기록

아티팩트: https://claude.ai/code/artifact/66481dfc-fea9-4c7e-8051-5adf12384a77
소스는 `dashboard/risk-console.html` — 수정 후 같은 URL로 재배포하고, 이 파일에 버전을 한 줄씩 추가한다.
버전업은 요청 시점이 아니라 **실제로 코드 반영 + 재배포까지 끝난 시점**에 한다.

## v1.11 — 2026-09-03
View/Index의 주가·매크로 차트를 캔들차트(빨강=상승/파랑=하락)로 전환.
- `collect.py`가 야후 차트 API에서 이미 받고 있던 open/high/low를 이제
  저장 — `fetch_yahoo_history()`가 종가만 뽑던 걸 OHLC 전체로 확장,
  `price_history.csv`/`macro.csv`에 컬럼 추가(기존 종가값은 절대 덮어쓰지
  않고 빈 open/high/low만 보완)
- `collect.yml`(매일 자동수집)에 그날 OHLC 보완 스텝 + `build_stock_charts.py`
  실행을 추가해 앞으로는 캔들 데이터가 자동으로 쌓임
- `build_stock_charts.py`를 `data/price_history.csv`(자동 수집, OHLC 포함)
  기준으로 전환하되, 기존 `dashboard/price_history.csv`(2025-11~ 수동
  1회성 종가)를 더 이전 구간 보완용으로 병합 — 9개월 이력 유지
- `stockChartSVG()`에 `candle` 옵션 추가(몸통+꼬리), OHLC 없는 구간은
  자동으로 기존 라인차트로 폴백. 호버 툴팁에 시/고/저/종 표시
- `backfill-prices.yml`/`backfill-macro.yml`을 2026-01-02부터 재실행해
  전자·닉스 164일치, 매크로 10종 전체를 실제 캔들로 채움

## v1.10 — 2026-09-03
Summ. 탭 히어로 확장 — 전일比 종목별 요인, 최근 2주/월별 손익 추이 미니차트, KPI 카드 제목 4개 변경.
- 표면 누적손익 아래 "전일比 요인" 칩 추가 — `dashboard/build_dod_breakdown.py`가
  `data/positions_history.csv` 최근 2영업일로 종목별(신용 합산) pnl 델타 상위 5개를
  계산해 `DATA.dod_breakdown`으로 주입
- "최근 2주 손익 추이" 미니카드 — 영업일 10개, Y축 0.5억 단위 그리드, 날짜별 표식
  호버 시 툴팁(날짜·x.xx억원), 아래에 자동 생성 해석 2줄(구간 범위 하이라이트 ·
  전일比 밑줄 강조, `.mc-interp mark`/`u`)
- "{이번달} 누적손익(MTD)" 미니카드 — 월별 손익 추이를 연 누적(1월~현재월 running
  total)으로 표시(주황색), '전년/올해 누적손익(ytd)' 두 줄을 흐린 보조 텍스트로 추가
- 두 미니차트는 `miniPnlChartSVG()`/`wireMiniPnlCharts()` 공용 헬퍼로 구현 —
  `.pending` 설정 + 삽입 후 별도 wire 호출 패턴(기존 stockChartSVG와 동일 방식)
- 핵심 요약 KPI 카드 제목 4개 변경(상세 breakdown은 기존 그대로): 계좌
  순자산(대출포함)→계좌자산(대출포함/신용제외), 순수 자기자본(세금 제외)→
  전재산(세전), 실질 레버리지→전재산 比 레버리지 배수, 청산까지 여력→
  국장계좌 청산여력

## v1.09 — 2026-09-01
차트 툴팁 통일 + 버그 2건 수정 + View/Index AI 분석 + 매매패턴 요약 + Cashflow 월별 표.
- 손익·신용잔고 / 미장·ISA 손익 추이 차트 툴팁에 계좌·항목 라벨(예: "손익 :",
  "신용 :")과 x.xx억원 단위를 명시적으로 통일
- **버그 수정**: 청산까지 여력·현금 비중·실질 레버리지 미니차트 클릭 시
  `periodDesc` 계산이 `fullSrc`/`period` 없는 def에서도 무조건 실행되어
  `PERIODS.find(...).label`이 undefined 참조로 크래시하던 문제 — `!dfn.fullSrc`
  가드 추가로 해결(Playwright 전체 클릭 검증, 이전 3건 → 0건)
- **버그 수정**: Risk 탭 리스크 신호등 2열 그리드가 `.rule .principle`의
  `white-space:nowrap`으로 인한 CSS Grid `min-width:auto` 버그로 우측 카드가
  184px 밖으로 넘치던 문제 — `.rule { min-width: 0; }` 한 줄로 해결
- View/Index 탭 AI 요약 폰트 2pt 확대 + `<mark>`/`<u>` 기반 강조 스타일 추가
- 주가·이동평균선 / 매크로 지표 차트에 포인터 호버 시 일자별 값 툴팁 추가,
  Y축을 `niceTicks()`로 끝자리 깔끔한 레벨로 개선, 상단에 최근 1주~1개월
  단위 AI 분석 문단(지표 간 상관관계·특이사항 해석) 추가
- 매매일지 상단에 1주/2주/4주/12주(영업일 5/10/20/60) 매매패턴 요약 추가
- 매매일지에 매매 사유(memo/trigger) 미기재 항목은 넛지 문구로 기록 유도
- Cashflow 탭에 월별 마크다운 표 추가(월 · 신용이자 · 매도세금/수수료 ·
  강의료 · 배당금) — 신용이자/매도세는 이미 손익에 반영된 항목이고
  강의구독비만 추가 고려 대상이라는 비고 포함

## v1.08 — 2026-08-31
탭 5개로 재구성 + 차트 카드에 기간 토글 + 매크로 지표 신설.
- 탭 이름/순서: Summ. → View/Index → Lookback → Risk → Cashflow
  - View/Index(신규): 이번 주 투자 기조(Lookback에서 이관) · 주가·이동평균선
    (Summ.에서 이관) · 매크로 지표(신규, KOSPI·코스닥·NASDAQ·나스닥선물·SOX·
    미국10년물·원달러·엔달러·WTI·금 10종, data/macro.csv 전체 컬럼 동적 반영) ·
    Study(빈 플레이스홀더)
  - Cashflow(신규): 현금흐름 내역(Summ.에서 이관)
  - Summ.: hero·KPI·손익추이·계좌구조·포트폴리오·월간결산만 남김
  - Lookback: 매매일지·생각노트만 남김(뷰트래커는 View/Index로)
- 차트 카드 5개(손익·신용잔고 추이 / 전체 순자산 / 실질 Exposure 추이 /
  신용잔고 추이 / 미장·ISA 손익 추이)에 1개월/3개월/'26 토글 추가 — 카드별
  독립 상태. 토글 도입으로 중복이던 '최근 1개월 손익 추이'·'2026년
  손익·신용잔고 추이(연간)' 카드를 '손익 · 신용잔고 추이' 하나로 통합
- 매크로 지표 카드는 1개월/3개월/1년(rolling) 토글, 카드별 독립 상태
- `dashboard/build_macro_chart.py` 신규 — macro.csv의 date/source를 뺀 모든
  숫자 컬럼을 자동으로 카드화. 값이 2개 미만인 컬럼은 "자료 없음" 플레이스홀더

## v1.07 — 2026-08-28
히어로 섹션 재구성 — "표면 누적손익"을 유일한 대형 지표로 올리고 "실질 누적 순손익"은
보조 지표로 격하.
- "표면 누적손익" 라벨에 "(CoB M/D, 요일)" 인라인 표기(요일은 DATA.date로 동적 계산)
- 표면 값 바로 아래에 전일比 라인 추가 — hero_total_dod를 그대로 재사용(세액 추정치가
  당일 안 바뀌는 한 표면/실질의 전일 대비 증감액은 수학적으로 동일함)
- "실질 누적 순손익"은 우측 breakdown 행으로 이동, (−) 강의·구독비 왼쪽에 배치,
  다른 항목과 같은 폰트 크기 + 연한 초록(.bd-item-real)
- 배포 전 목업(스크린샷)으로 먼저 확인받고 "배포" 확정 후에만 버전 올리는 흐름을
  이번 턴부터 명시적으로 적용

## v1.06 — 2026-08-27
히어로(실질 누적 순손익) 섹션 확장 — 계좌별 기준원금·총평가액·세액·이자를 카드 안에서 바로 확인.
- 국장/미장/ISA 각각 4행: 손익 → 전일比 → 총평가액 → 기준원금(+계좌별 추가행)
  - 국장만 "총평가액(신용차감후)" 표기(신용 있는 계좌는 국장뿐), ISA/미장은 "총평가액"만
  - 국장 하단에 "※ 누적신용이자" 행 추가(상단 breakdown에서는 이 항목 제거)
  - 미장은 세전(surface_pnl) 금액을 메인으로 표시, "세후" 배지 제거, 대신
    "※ 예상세액" 행 추가 — `cum_tax_est`는 올해 실현 해외주식손익(99,059,995원)
    기준으로 갱신: (99,059,995 − 250만) × 22% = 21,243,199원
  - 상단 총계에 "CoB M/D" 라벨 추가, "전일比"에서 날짜 괄호 제거해 간결화
- `data/daily.csv` 8/26 국장 값 보정(+4,777,504원) — 8/26→8/27 dod가 사용자 확인
  실측치(+21,146,061원)와 맞도록, SKT 매매 실현손익 누락분으로 추정해 반영
- 자동수집 파이프라인이 다른 세션에서 이 아티팩트를 stock_charts 없이 재배포한 걸
  발견해서, 배포 직전에 build_stock_charts.py로 복원 후 진행
- journal.csv의 realized_pnl 컬럼이 대부분 비어 있어(5건·137만원) 매매내역 기반
  세액 자동추정은 아직 근거 부족 — 당분간 사용자 확정치를 받아 반영하는 방식 유지

## v1.05 — 2026-08-24
핵심요약 탭 · 포트폴리오 구성 아래에 SK하이닉스·삼성전자 주가 차트 추가.
- `dashboard/fetch_price_history.py` — 야후 파이낸스에서 9개월 종가 조회(샌드박스는
  야후/네이버 접근이 막혀 있어 사용자가 로컬에서 직접 실행, 결과는
  `dashboard/price_history.csv`로 저장 후 별도 브랜치로 push)
- `dashboard/build_stock_charts.py` — 20/60/120일 이동평균 계산해 `DATA.stock_charts`로
  주입. 화면은 최근 3개월만 표시하되 이평선은 9개월 전체로 계산해 워밍업 구간을 최소화
- **주의: 실시간/데일리 자동갱신 아님.** 가격은 2026-08-24 기준 1회성 스냅샷이고,
  이후 자동으로 새로고침되지 않는다. 갱신하려면 사용자가 로컬에서
  `fetch_price_history.py` 재실행 → CSV 전달 → `build_stock_charts.py` 재실행 →
  "배포" 3단계를 다시 거쳐야 한다.

## v1.04 — 2026-08-24
- `dashboard/build_dash_data.py` 신규 — `full_pnl`/`full_eq`/`full_expo`/`full_credit`/
  `full_lev_real` 5개 전체이력 배열을 `data/daily.csv`+`data/cashflow.csv`에서
  다시 계산해 재주입하는 스크립트. 아티팩트에 박혀있던 값이 2026-01~08 국장/ISA
  백필 이전 스냅샷이라 2026년 손익 포인트가 9개뿐이었던 문제 해결 — 재실행 후
  165개로 채워짐(6~7월 반등→반대매매 직전 급락 구간이 그대로 드러남, 실제 이력임)
- 차트 토글: '최근 3개월 손익 추이' 삭제, '2026년 손익(연간)'·'2026년 신용잔고(연간)'
  2개를 듀얼축 1개 차트('2026년 손익 · 신용잔고 추이(연간)')로 통합
- 핵심요약 탭: 월간결산 섹션을 포트폴리오 구성 아래로 이동
- 현금흐름 내역에 시각화 추가 — 누적 비용·수익 항목(신용이자/수수료/매도세금/
  강의비/배당금) 가로 막대 + 월별 원금 순증감 diverging 막대 차트, 기존 상세
  리스트는 하단에 그대로 유지
- 버전업 정책 확정: 요청받을 때마다 바로 배포하지 않고, 사용자가 "배포"라고
  명시할 때만 버전 번호를 올리고 재배포한다

## v1.03 — 2026-08-24
탭 구성을 사용자 요청대로 재배치. 1번 탭을 그래프 중심 "핵심요약"으로,
2번 탭을 "리스크 관리 현황"으로 바꿈 (3번 "일지·기록"은 그대로).
- **핵심요약**: 실질 순손익 히어로 · KPI · 월간결산 · 누적손익 추이(9종 차트 토글,
  계좌별 자본구조 포함) · 포트폴리오 구성 · 현금흐름 내역 — "손익·추이"에 해당하는
  모든 그래프·표를 이 탭 하나로 모음
- **리스크 관리 현황**: 해야 할 액션 · 리스크 신호등(8개 룰) · 대원칙
- 손익·신용잔고 **2026년 연간 추이 차트 2종 신규 추가**(기존엔 1개월/3개월 확대 뷰만
  있었음) — `full_pnl`/`full_credit`(전체 493일 이력)를 `date.slice(0,4)==='2026'`로
  필터링해 차트 토글 목록에 추가, 클릭하면 큰 차트로 전환됨

## v1.02 — 2026-08-24
탭 3개로 분리 (기존 요소는 전부 그대로 유지, 배치만 재구성).
- **리스크 현황**: 실질 순손익 히어로 · 핵심요약 KPI · 해야 할 액션 · 포트폴리오 구성 · 리스크 신호등 · 대원칙
- **손익·추이 분석**: 월간결산 · 현금흐름 내역 · 누적손익 추이(7종 차트 토글) · 계좌별 자본 구조
- **일지·기록**: 매매일지 · 이번 주 투자 기조(뷰트래커) · 생각노트 · 개선 아이디어 백로그
탭 선택 상태는 브라우저 로컬저장소에 기억됨(`localStorage`, 뷰어별로 독립).

## v1.01 — 2026-08-24 (기준선)
탭 분리 이전 상태를 그대로 저장소에 백업 (`dashboard/risk-console.html`, 커밋 `5348e9b`).
그 이전 버전은 git에 남아있지 않음 — claude.ai 아티팩트로만 존재했음.
